import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_main/settings/_main/branches')({
  component: () => <div>Hello /_main/settings/_main/branches!</div>,
})
